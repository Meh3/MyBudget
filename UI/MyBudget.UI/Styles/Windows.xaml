<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviour="clr-namespace:MyBudget.UI.Behaviors"
    xmlns:ext="clr-namespace:MyBudget.UI.ControlExtensions"
    xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity">

    <ControlTemplate x:Key="Tempalte.Window.Main" TargetType="{x:Type Window}">
        <Grid
            Width="{TemplateBinding Width}"
            Height="{TemplateBinding Height}"
            Background="{StaticResource Brush.Background}">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border
                Grid.Row="0"
                Grid.RowSpan="2"
                BorderBrush="{StaticResource Brush.Background.Line}"
                BorderThickness="{StaticResource Thickness.Border}" />

            <Grid
                x:Name="WindowBar"
                Grid.Row="0"
                Height="50"
                Margin="2,2,2,0"
                Background="Transparent">
                <i:Interaction.Behaviors>
                    <behaviour:ControlDragsWindow />
                </i:Interaction.Behaviors>
                <ext:UIButton
                    Width="50"
                    HorizontalAlignment="Right"
                    Background="Transparent"
                    Content="{StaticResource PathGometry.Corner.TopRight}"
                    Style="{StaticResource Style.Button.Path.Warning}">
                    <i:Interaction.Behaviors>
                        <behaviour:ButtonClickClosesWindow />
                    </i:Interaction.Behaviors>
                </ext:UIButton>
                <TextBlock
                    Padding="0,0,4,0"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    FontSize="15"
                    FontWeight="Medium"
                    Foreground="DarkRed"
                    IsHitTestVisible="False"
                    Text="X" />
                <Path
                    Width="100"
                    HorizontalAlignment="Left"
                    Data="{StaticResource PathGometry.Corner.TopLeft}"
                    Fill="{StaticResource Brush.Background.Shape}"
                    Stretch="Fill"
                    Stroke="{StaticResource Brush.Background.Shape}"
                    StrokeLineJoin="Round"
                    StrokeThickness="2" />
            </Grid>

            <Grid
                x:Name="WindowContent"
                Grid.Row="1"
                Margin="2,0,2,2">
                <ext:UIButton
                    Width="15"
                    Height="15"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom"
                    Style="{StaticResource Style.Button.Base}">
                    <Grid>
                        <Path Data="{StaticResource PathGometry.Corner.BottomRight}" Style="{StaticResource Style.Path.Neon}" />
                        <Thumb>
                            <i:Interaction.Behaviors>
                                <behaviour:ThumbResizesWindow />
                            </i:Interaction.Behaviors>
                            <Thumb.Template>
                                <ControlTemplate>
                                    <Grid Background="Transparent" />
                                </ControlTemplate>
                            </Thumb.Template>
                        </Thumb>
                    </Grid>
                </ext:UIButton>
            </Grid>
        </Grid>
    </ControlTemplate>

    <Style x:Key="Style.Window.Main" TargetType="{x:Type Window}">
        <Setter Property="WindowStyle" Value="None" />
        <Setter Property="MinWidth" Value="300" />
        <Setter Property="MinHeight" Value="400" />
        <Setter Property="AllowsTransparency" Value="True" />
        <Setter Property="Template" Value="{StaticResource Tempalte.Window.Main}" />
    </Style>
</ResourceDictionary>